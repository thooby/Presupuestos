<%= form_for(@mini) do |f| %>
  <% if @mini.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@mini.errors.count, "error") %> prohibited this mini from being saved:</h2>

      <ul>
      <% @mini.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :codigo %><br />
    <%= f.text_field :codigo %>
  </div>
  <div class="field">
    <%= f.label :desc %><br />
    <%= f.text_field :desc %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
