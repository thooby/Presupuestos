<h1>Listing minis</h1>

<table>
  <tr>
    <th>Codigo</th>
    <th>Desc</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @minis.each do |mini| %>
  <tr>
    <td><%= mini.codigo %></td>
    <td><%= mini.desc %></td>
    <td><%= link_to 'Show', mini %></td>
    <td><%= link_to 'Edit', edit_mini_path(mini) %></td>
    <td><%= link_to 'Destroy', mini, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Mini', new_mini_path %>
