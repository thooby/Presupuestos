<%= form_for(@compara) do |f| %>
  <% if @compara.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@compara.errors.count, "error") %> prohibited this compara from being saved:</h2>

      <ul>
      <% @compara.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :asiento_id %><br />
    <%= f.text_field :asiento_id %>
  </div>
  <div class="field">
    <%= f.label :orden %><br />
    <%= f.text_field :orden %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
